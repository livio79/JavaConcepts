package poker;

import java.util.*;


public class PokerTest {
	
	private int numberOfPlayers = 5;
	
	
	public int getNumberOfPlayers() {
		return numberOfPlayers;
	}

	public static void main(String[] args) { //trasformalo in play(int numero giocatori)
		 /* Card c = new Card();
		  Player p = new Player();
		  
		  
		  int nPlayers = new PokerTest().getNumberOfPlayers();
		
		Player  [] players = p.createPlayer(nPlayers);		//5 e il numero di giocatori!!!!!!!!!!!!!
		ArrayList<Card> deck = c.createDeck(true); 		//creo Mazzo mischiato
		
		
			for (int i = 0; i < nPlayers; i++) {
				c.giveCArds(players[i], deck);
				Card [] mazzetto = players[i].getCards();
				
			
				c.sortDeck(mazzetto);
				
				c.printFive(players[i]);
				System.out.println();
				}
			
			String [] a = {" _ ", "| |", "|_|"};
			for (int i = 0; i < 3; i++) {
			for (int j = 0; j < 5; j++) {
				System.out.print(a[i] + " ");
			}
			System.out.println();
		}
			
		*/
		
						
}
}


//numero di mani opzionale - punto in mano - soldi -  propensione al rischio -joptionPane per immetere dati in sicurezze e/o non incasinare troppo la console -
